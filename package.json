{
  "name": "firstrepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development next dev --hostname 0.0.0.0 --port 3000",
    "build": "NODE_ENV=production next build",
    "start": "NODE_ENV=production next start --hostname 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "next": "^16.2.6",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  }
}
