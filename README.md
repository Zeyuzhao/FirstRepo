Weekly Operations Dashboard
===========================

A small static executive dashboard for weekly support and fulfillment performance.

The current sandbox has no configured SQL connection, so the dashboard uses a small mock dataset in
`app.js` to preview the layout. Open `index.html` directly in a browser or serve the directory with
any static file server.
