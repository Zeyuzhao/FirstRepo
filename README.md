FirstRepo
=========

Live SQL dashboard for the configured Jiro Postgres database.

## Run

```bash
npm install
npm start
```

The app reads `TEST_DATABASE_URL` or `DATABASE_URL` and serves a live dashboard at
`http://localhost:3000`.
