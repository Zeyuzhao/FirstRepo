# FirstRepo

Minimal Bun + Next.js app using the App Router.

## Scripts

- `bun run dev` - start local development server on `0.0.0.0:3000`
- `bun run build` - create production build
- `bun run start` - start production server on `0.0.0.0:3000`
