FirstRepo
=========

This is a lightweight static operations dashboard built from the provided CSV data.

Open `index.html` directly in a browser, or serve the folder locally:

```bash
python3 -m http.server 3000 --bind 0.0.0.0
```
